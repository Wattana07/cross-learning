# ✅ Checklist: ตั้งค่าระบบส่งอีเมลให้ทำงานได้จริง

## ขั้นตอนที่ 1: ตั้งค่า Resend API Key ใน Supabase

### ⚠️ สำคัญมาก! ต้องทำก่อน

1. ไปที่: https://supabase.com/dashboard/project/wmfuzaahfdknfjvqwwsi/settings/functions
2. คลิก **"Secrets"** tab
3. คลิก **"New secret"**
4. ใส่ข้อมูล:
   ```
   Name: resend_api_key
   Value: re_j3xSDNVe_GpS257HZTdT7iub22ZeE4s3r
   ```
5. คลิก **"Create secret"**

**⚠️ หมายเหตุ:** 
- ชื่อต้องเป็นตัวพิมพ์เล็ก: `resend_api_key` (ไม่ใช่ `RESEND_API_KEY`)
- หลังจากตั้งค่าแล้ว อาจต้องรอสักครู่ให้ระบบอัปเดต

---

## ขั้นตอนที่ 2: ตรวจสอบว่า Functions ถูก Deploy

รันคำสั่งนี้:

```bash
cd C:\Users\USER\Documents\cores\cross-learning
npx supabase functions list
```

ควรเห็น:
- ✅ `send-email` - ACTIVE
- ✅ `create-user` - ACTIVE

---

## ขั้นตอนที่ 3: ทดสอบ

1. **เพิ่ม user ใหม่:**
   - ไปที่ Admin > Users > เพิ่มผู้ใช้ใหม่
   - กรอกข้อมูล: อีเมล, ชื่อ-นามสกุล
   - คลิก "สร้างผู้ใช้"

2. **ตรวจสอบผลลัพธ์:**
   - ✅ ถ้าสำเร็จ: จะเห็น Toast "สร้างผู้ใช้สำเร็จ! อีเมลตั้งรหัสผ่านถูกส่งไปยัง..."
   - ❌ ถ้าไม่สำเร็จ: จะเห็น Toast error message

---

## ขั้นตอนที่ 4: ตรวจสอบ Logs (ถ้ายังไม่ได้รับอีเมล)

1. ไปที่: https://supabase.com/dashboard/project/wmfuzaahfdknfjvqwwsi/functions
2. คลิก **"Logs"** tab
3. เลือก function: `create-user` หรือ `send-email`
4. ดู error messages

**Error ที่พบบ่อย:**
- `RESEND_API_KEY is not set` → ยังไม่ได้ตั้งค่า Secret
- `EMAIL_SERVICE_NOT_CONFIGURED` → API Key ไม่ถูกต้อง
- `EMAIL_SEND_FAILED` → ดู error details ใน logs

---

## ขั้นตอนที่ 5: ตรวจสอบ Resend Dashboard

1. ไปที่: https://resend.com/emails
2. ดูว่ามีอีเมลถูกส่งหรือไม่
3. ดู status: sent, delivered, bounced
4. ดู error messages (ถ้ามี)

---

## ขั้นตอนที่ 6: ตรวจสอบอีเมล

1. ตรวจสอบ **Inbox**
2. ตรวจสอบ **Spam/Junk folder** (สำคัญ!)
3. ตรวจสอบว่า email address ถูกต้อง

---

## 🆘 ถ้ายังไม่ทำงาน

### วิธีที่ 1: ตรวจสอบ Logs

1. ไปที่ Supabase Dashboard > Edge Functions > Logs
2. ดู logs ของ `create-user` และ `send-email`
3. คัดลอก error message ทั้งหมด
4. ส่งมาให้ฉันดู

### วิธีที่ 2: ทดสอบ send-email โดยตรง

ใช้ curl:

```bash
curl -X POST https://wmfuzaahfdknfjvqwwsi.supabase.co/functions/v1/send-email \
  -H "Authorization: Bearer YOUR_SERVICE_ROLE_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "to": "your-email@example.com",
    "subject": "Test Email",
    "html": "<h1>Test</h1>"
  }'
```

---

## ✅ สรุป Checklist

- [ ] ตั้งค่า `resend_api_key` ใน Supabase Secrets แล้ว
- [ ] Functions ถูก deploy แล้ว (`send-email`, `create-user`)
- [ ] ลองเพิ่ม user ใหม่แล้ว
- [ ] ตรวจสอบ Logs แล้ว
- [ ] ตรวจสอบ Resend Dashboard แล้ว
- [ ] ตรวจสอบอีเมล (Inbox และ Spam) แล้ว

---

## 📞 ถ้ายังแก้ไม่ได้

ส่งข้อมูลนี้มา:
1. Error message จาก Logs
2. Screenshot จาก Resend Dashboard
3. ว่า Secret ถูกตั้งค่าแล้วหรือยัง

