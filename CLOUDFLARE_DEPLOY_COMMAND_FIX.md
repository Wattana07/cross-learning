# แก้ไข Deploy Command ที่บังคับให้กรอก

## ✅ วิธีแก้ไข

ถ้า Cloudflare Pages บังคับให้กรอก Deploy command ให้ใส่คำสั่งนี้:

```
true
```

หรือ

```
echo "Deploying to Cloudflare Pages..."
```

---

## 📝 คำอธิบาย

- `true` = คำสั่งที่ทำอะไรไม่ทำ แค่ return success
- `echo` = แค่แสดงข้อความ ไม่ทำอะไร

**Cloudflare Pages จะ deploy `dist` folder อัตโนมัติอยู่แล้ว** ไม่ต้องใช้ Deploy command จริงๆ

---

## 🔧 ขั้นตอน

1. **ใส่ Deploy command**: `true`
2. **คลิก "Deploy"**
3. **หลังจาก deploy เสร็จ**:
   - ไปที่ Settings > Builds & deployments
   - ลบ Deploy command ออก (เว้นว่าง)
   - Save

---

## ⚠️ หมายเหตุ

- Deploy command ที่ใส่ (`true`) จะไม่ทำอะไร
- Cloudflare Pages จะ deploy `dist` folder อัตโนมัติ
- หลังจาก deploy สำเร็จแล้ว สามารถลบ Deploy command ออกได้

