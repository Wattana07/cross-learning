# 🔒 แก้ปัญหา: ลิงก์ในอีเมลขึ้นว่า "ลิงก์นี้ดูอันตราย"

## ❌ ปัญหา

เมื่อคลิกลิงก์ในอีเมล จะขึ้น warning:
- "ลิงก์นี้ดูอันตราย"
- ลิงก์ชี้ไปที่ `us-east-1.resend-clicks.com`

**สาเหตุ:** Resend ใช้ click tracking โดย default ซึ่งจะ wrap ลิงก์ผ่าน domain ของ Resend

---

## ✅ วิธีแก้ไข

### Step 1: ตรวจสอบว่าโค้ดมี `click_tracking: false` แล้ว

โค้ดควรมี:
```typescript
body: JSON.stringify({
  from: fromEmail,
  to: email,
  subject: `...`,
  html: emailHtml,
  text: emailText,
  click_tracking: false,  // ← ต้องมี
  open_tracking: false,   // ← ต้องมี
}),
```

### Step 2: Deploy Edge Function ใหม่

**สำคัญมาก!** ต้อง deploy ใหม่เพื่อให้การเปลี่ยนแปลงมีผล:

```bash
cd C:\Users\USER\Documents\cores\cross-learning
npx supabase functions deploy create-user
```

### Step 3: ทดสอบ

1. **ลบอีเมลเก่าทั้งหมด** (ถ้ามี)
2. **เพิ่ม user ใหม่** เพื่อให้ส่งอีเมลใหม่
3. **ตรวจสอบลิงก์ในอีเมล:**
   - ลิงก์ควรชี้ไปที่ `https://cross-learning.vercel.app/login` โดยตรง
   - ไม่ควรผ่าน `resend-clicks.com`

---

## 🔍 ตรวจสอบว่า Deploy สำเร็จ

1. **ตรวจสอบใน Supabase Dashboard:**
   - ไปที่ Supabase Dashboard > Edge Functions
   - ดูว่า `create-user` เป็น ACTIVE
   - ดู Last Deployed time ว่าอัปเดตล่าสุด

2. **ตรวจสอบ Logs:**
   ```bash
   npx supabase functions logs create-user
   ```
   - ดูว่า `click_tracking: false` ถูกส่งไปใน request หรือไม่

---

## 💡 Tips

- **อีเมลเก่า:** อีเมลที่ส่งไปก่อน deploy จะยังใช้ click tracking อยู่
- **อีเมลใหม่:** อีเมลที่ส่งหลังจาก deploy จะไม่ใช้ click tracking
- **Browser Cache:** ถ้ายังเห็น warning อาจต้อง clear browser cache

---

## ✅ Checklist

- [ ] ตรวจสอบว่าโค้ดมี `click_tracking: false` และ `open_tracking: false`
- [ ] Deploy Edge Function `create-user` ใหม่
- [ ] ตรวจสอบว่า deploy สำเร็จ
- [ ] ลบอีเมลเก่า (ถ้ามี)
- [ ] เพิ่ม user ใหม่เพื่อทดสอบ
- [ ] ตรวจสอบลิงก์ในอีเมลใหม่ว่าไม่ผ่าน `resend-clicks.com`

